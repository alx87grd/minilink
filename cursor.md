# Cursor Instructions

Read and follow [`agent.md`](agent.md). It is the source of truth for Minilink's AI-agent workflow, math-first coding style, notebook policy, and approval boundaries.

User-facing workflow documentation lives in [`README.md`](README.md). Implementation call paths are in [`flows.md`](flows.md).

The Cursor rule in `.cursor/rules/read-agent-first.mdc` exists only to point agents back to `agent.md`.
