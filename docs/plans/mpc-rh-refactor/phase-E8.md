# Phase E8 — Broadcast + other backends

**Status:** stub (expand before coding)  
**Depends on:** earlier phases green · contracts: [vision.md](vision.md)

## Goal

Multi-rate nominal broadcast (`u_nom`/`x_nom`); optional fill `x_dot`/`u_dot`;
smoke `solve_trajectory_from` on batch TOP / RRT; DP rollout → traj only if
needed.

## Exit / gate

Targeted new tests; one multi-rate hybrid or hand-loop broadcast demo.
