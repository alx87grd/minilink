"""Benchmark helpers: ``f`` evaluator timings and ``Simulator`` speed matrices.

Use :mod:`minilink.benchmark.f_speed` and :mod:`minilink.benchmark.simulation_speed`
for concrete APIs. Shared stress scenarios and builders live under
:mod:`minilink.benchmark.scenario`. Example runners (flat scripts, not pytest) live
under ``tests/benchmark/``.
"""
